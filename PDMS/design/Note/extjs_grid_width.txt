----------------------------------------------------------
Ext GridPanel 宽度设置问题

首先
viewConfig : {
            forceFit : true
        },
可以做到显示所有列。


方法1：
var grid=new Ext.grid.GridPanel(...);

var view=new Ext.Viewport({
layout:'border',
renderTo:'topic-grid',
border:false,
items:[{
     region:'center',
     layout:'fit',
     items:[grid]
}]
});

方法2：
<div id="tt" style="width:100%;" ></div> 
设置grid
width: Ext.get("tt").getWidth(), 

